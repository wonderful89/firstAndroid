<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.firstAndroid"
    android:sharedUserId="android.uid.test1">
    <!-- bugly升级：必须的权限配置 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".functions.ui.layout_test_1.LayoutTest_1Activity"
            android:exported="true" />
        <activity android:name=".functions.components.WebViewX5Activity" />
        <activity android:name=".functions.components.WebViewOriginActivity" />
        <activity android:name=".functions.ui.dagger.Dagger2Activity" />
        <activity android:name=".functions.ui.dagger.Dagger1Activity" />
        <activity android:name=".functions.ui.anko.Anko2Activity" />
        <activity android:name=".functions.ui.anko.Anko1Activity" />
        <activity android:name=".functions.ui.animation.UIAnimate2Activity" />
        <activity android:name=".functions.ui.animation.UIAnimate1Activity" />
        <activity android:name=".functions.ui.touchMe.UITouchMeActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->
            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".functions.ui.UITestView1Activity" />
        <activity android:name=".functions.ui.listview.ListViewActivity1" />
        <activity android:name=".base.BaseActivity" />
        <activity android:name=".functions.ui.listview.TestListViewActivity" />
        <activity android:name=".functions.util.UtilTestActivity" />
        <activity android:name=".functions.components.ComponentActivity" />
        <activity android:name=".functions.logic.LogicListActivity" />
        <activity android:name=".functions.ui.UITestListActivity" />
        <activity android:name=".functions.test.TestActivity1" />
        <activity android:name=".functions.ui.layout_test_1.LayoutTest1Activity" />
        <activity
            android:name=".TestListActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">

            <!-- 该Activity是该应用的启动入口页面，它会被储存在系统的launcher列表中 -->
            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->


            <!-- 该Activity能够处理ACTION_SEND行为且数据类型为text/plain的隐式Intent -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
            </intent-filter>
            <!-- 该Activity能够处理ACTION_SEND行为且数据类型是媒体内容的隐式Intent -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <action android:name="android.intent.action.SEND_MULTIPLE" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="image/*" />
                <data android:mimeType="video/*" />
            </intent-filter>
        </activity>
        <activity
            android:name=".functions.ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".MainActivity"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- bugly升级：必须配置 -->
        <activity
            android:name="com.tencent.bugly.beta.ui.BetaActivity"
            android:configChanges="keyboardHidden|orientation|screenSize|locale"
            android:theme="@android:style/Theme.Translucent" /> <!-- bugly升级：必须配置 fileProvider， 注意： android:authorities="${applicationId}.fileProvider" 不要直接copy，设置为你的包名.fileProvider -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.firstAndroid.fileProvider3"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
    </application>

</manifest>
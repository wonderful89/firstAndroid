<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.firstAndroid">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".functions.ui.listview.ListViewActivity1">
            <!-- 该Activity是该应用的启动入口页面，它会被储存在系统的launcher列表中 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".base.BaseActivity" />
        <activity android:name=".functions.ui.listview.TestListViewActivity" />
        <activity android:name=".functions.util.UtilTestActivity" />
        <activity android:name=".functions.components.ComponentActivity" />
        <activity android:name=".functions.logic.LogicListActivity" />
        <activity android:name=".functions.ui.UITestListActivity" />
        <activity
            android:name=".TestListActivity"
            android:label="@string/main_name"
            android:theme="@style/AppTheme">

            <!-- 该Activity能够处理ACTION_SEND行为且数据类型为text/plain的隐式Intent -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
            </intent-filter>
            <!-- 该Activity能够处理ACTION_SEND行为且数据类型是媒体内容的隐式Intent -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <action android:name="android.intent.action.SEND_MULTIPLE" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="image/*" />
                <data android:mimeType="video/*" />
            </intent-filter>
        </activity>
        <activity
            android:name=".functions.ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".MainActivity"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>